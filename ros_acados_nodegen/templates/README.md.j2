# {{ package.name }}

This package is generated by the [ros_acados_nodegen]() package. 

## Overview
This package contains the ROS node generation for acados, which is a high-performance solver for optimal control problems. The generated nodes can be used to interface with acados solvers in a ROS environment.


## Installation
To install this package, you can use the following command:
```bash
rosdep install --from-paths src --ignore-src -r -y
```

```bash
colcon build --packages-select {{ package.name }} && source install/setup.bash
```
e.g.:
```bash
colcon build --packages-select {{ package.name }} --event-handlers console_direct+ --cmake-args -DCMAKE_EXPORT_COMPILE_COMMANDS=1
```

## Usage
After building the package, you can run the generated nodes using:
```bash
ros2 run {{ package.name }} {{ ros.node_name }}
```


